# MEGA's RPM scriptlet tries to raise this at install time via sysctl, which
# fails in a container build. Setting it declaratively here does the same job
# and applies on every boot.
#
# MEGAsync needs a watch per synced directory; the Fedora default is low
# enough that large sync trees silently stop updating.
fs.inotify.max_user_watches = 524288
