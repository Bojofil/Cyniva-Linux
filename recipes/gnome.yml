# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: cyniva-linux-gnome            # <-- change me; this becomes the image name
description: Cyniva Linux GNOME
alt-tags:
  - gnome

base-image: ghcr.io/ublue-os/bluefin
image-version: latest     # or a pinned Fedora version tag, e.g. "44"

modules:
  - from-file: common.yml
